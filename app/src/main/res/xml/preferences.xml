<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:app="http://schemas.android.com/apk/res-auto">

    <ListPreference
        app:key="dark_mode"
        app:title="Theme"
        app:summary="%s"
        app:entries="@array/theme_entries"
        app:entryValues="@array/theme_values"
        app:defaultValue="auto" />

    <SwitchPreferenceCompat
        app:key="hide_expensive_further"
        app:title="Hide expensive farther stations"
        app:summary="Filters out stations that are more expensive than nearer ones (Recommended)"
        app:defaultValue="true" />

    <SwitchPreferenceCompat
        app:key="only_public_sale"
        app:title="Hide prices for members"
        app:summary="Show only public prices"
        app:defaultValue="true" />

    <SwitchPreferenceCompat
        app:key="filter_by_opening_hours"
        app:title="Hide stations"
        app:summary="Do not show stations not open right now."
        app:defaultValue="true" />

</PreferenceScreen>
